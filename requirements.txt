# Core runtime (CPU-only, zero-cost). Pinned to the versions CI + reproduce.sh
# run against, so numbers reproduce. Bump deliberately, not incidentally.
requests==2.33.1     # ChEMBL REST client (honors HTTPS_PROXY)
pandas==3.0.5        # candidate tables
pyarrow==24.0.0      # parquet cache backend
rdkit==2026.3.4      # SMILES validation + drawing
scikit-learn==1.9.0  # HistGradientBoosting QSAR
streamlit==1.60.0    # dashboard

# Dev / test
pytest==9.1.1
