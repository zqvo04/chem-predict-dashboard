# ROADMAP — 통합 개선 설계

**이 문서의 규칙: 목표·순서·게이트만 적는다. 측정은 [STATE.md](STATE.md)의 일이다.**

이 문서는 `DMTA_DESIGN.md` · `IMPROVEMENT_PLAN.md` · `E2E_COMPLETION.md` ·
`PHASE3_DESIGN.md` · `DIAGNOSIS.md` 다섯 문서를 대체한다. 원본은
[`docs/archive/`](docs/archive/)에 있으며, 삭제하지 않은 이유는 §1에 있다.

작성: 2026-08-03

---

## 0. 방향

> **자기 예측의 신뢰도를 아는 선택성 예측기.**
> 예측(1)과 신뢰도(2)에 더해, **이 예측이 틀렸다는 것을 어떻게 알게 될 것인가(3)를 항상 함께 낸다.**

대부분의 도구가 (1)을 하고, 좋은 도구가 (2)까지 한다. **(3)이 이 저장소의 자리다.**
그리고 이것은 취향이 아니라 제약의 논리적 귀결이다 — 공개 데이터 10⁴로는 규모 경쟁이
불가능하고, 데이터가 적을수록 **언제 틀리는지 아는 것이 전부**가 된다.

**하지 않는 것 (똑같이 중요):**

| 안 함 | 왜 |
|---|---|
| 규모 경쟁 (10⁶ 라이브러리) | 공개 10⁴ vs 사내 10⁶. 지고, **질 필요도 없다** — 병목은 크기가 아니라 채점 정확도다 |
| 범용 신약탐색 플랫폼 | 검증 불가. v1이 그래서 폐기됐다 |
| 합성 · IP · PK/PD · 임상 | 범위 밖 (§9) |
| 독성/liability 모델링 | 현재는 밖. hERG를 게이트 음성에서 빼면 게이트가 재학습되고 **G0가 깨진다** |
| **"AI가 신약을 발견했다"** | 이 저장소는 **검증 가능한 예측을 공개**할 뿐이다 |

### 우선순위 (프로젝트 오너 결정)

**역량 > 취업 > 툴 > 논문.** 논문이 4순위이므로 **패널 일반화가 필수에서 선택으로 내려간다** —
PI3K 검열 오라클 39개 문제(STATE §6d)를 지금 정면으로 풀 필요가 없다.

---

## 1. 이 문서가 대체한 것 — 반증 체인

다섯 문서는 서로 모순되지 않았다. **시간순으로 쌓인 3단계 반증 체인**이었고, 어느 층이
아직 유효한지 표시가 없었을 뿐이다.

```
PHASE3_DESIGN    "획득함수를 벤치마크한다"            (회고 오라클을 전제)
   ↑ 반증
DMTA_DESIGN §1   "그 오라클 모집단은 9~28개다"        → 오라클 3종으로 재설계
   ↑ 반증
IMPROVEMENT_PLAN "루프보다 potency 축이 91 % 틀렸다"  → 우선순위 뒤집음
   ↑ 반증
DIAGNOSIS §1a    "그 계획이 전제하는 재생성 경로가 이미 부러져 있다"
```

`E2E_COMPLETION`은 이 체인 밖의 별도 축(라벨 → 게이트 → 건초더미 순서 제약)이고 살아남았다.

**아카이브를 삭제하지 않는 이유:** 각 층이 앞 층을 어떻게 반증했는지가 이 프로젝트의
자산이다. 평평하게 합치면 "왜 이 순서인가"의 근거가 사라진다. 각 원본 머리에 무엇이
그것을 대체했는지 한 줄을 붙였다.

---

## 2. 전체 파이프라인

```
┌ Stage 0   PROGRAM ─────────────────────────── [대화 + 공개 API, 사람 서명]  신규
│   적응증 → primary / anti-target → 목적함수 부호
│   ⚠ 기록된 격차: 적응증 암묵(7a) · JAK3 근거 없음(7b) · TYK2 부재(7c)
│
├ Stage 0.5 적합성 스크린 ───────────────────── [스크립트, CI 가능]           신규
│   파라로그 ≥3 / 교차측정 / 검열 모집단 / 누출 / 다양성
│   ← 이것을 만들면 생산 경로 수리가 강제되고 검증된다
│
├ Stage 1   DATA ────────────────────────────── ✅ 증거저장소 (ChEMBL_37, 타깃 7개)
├ Stage 2   MODEL ───────────────────────────── ⚠ potency 축 91.3 % 반증
│                                          [GPU] chemprop 멀티태스크 (조건부)
├ Stage 3   DESIGN ──────────────────────────── 라이브러리 스크리닝 / 생성
│                                          [GPU] 생성 모델 (도킹 통과 시에만)
├ Stage 4   MAKE ────────────────────────────── SA + 드러그라이크
│           + **구매가능성** ← 승격. 기여 경로의 전제
│
├ Stage 5   TEST ────────────────────────────── 검열(414 봉인) / 시간분할(1,525)
│           + **ChEMBL 릴리스 증분** ← 진짜 새 정보의 유일한 원천
│                                          [GPU] 도킹 = 직교 바인더 증거
├ Stage 6   ANALYZE ─────────────────────────── 반증률 / 재학습 트리거 / 정지규칙
│
└ Stage 7   예측 등록 + 전향 검증 ───────────── 신규
     전체 랭킹을 타임스탬프·해시·model_id와 함께 커밋
     릴리스마다 자동 채점 → 맞든 틀리든 STATE.md
     ↳ 구매 가능 분자는 ManualOracle로 실측 유입
```

### 왜 Stage 7인가 — "실제 기여"의 정직한 형태

분자는 인식론적으로 세 종류다.

| 분류 | 검증 | 규모 |
|---|---|---|
| ① ChEMBL에 JAK 측정 있음 | 조회로 즉시. 단 학습셋에 있으면 **회상** | 학습셋 밖 **9~28개** |
| **② ChEMBL에 있으나 JAK 측정 없음** | **예측이 진짜 예측. 누군가 사서 시험 가능** | **38,592개 (와이드 라이브러리)** |
| ③ 어디에도 없는 신규 분자 | **합성 없이는 불가** | 생성 모델 산출 |

**기여는 ③이 아니라 ②에서 나온다.** ②의 분자는 실재하고, 합성 가능하며(누가 이미 만들었다),
JAK에 대해서만 아무도 안 재봤다. 그리고 검증 경로가 둘 있다 — **릴리스 증분(무료, 느림,
전향적)** 과 **구매 + 어세이(유료, 통제 가능)**.

> **사전등록의 핵심 성질: git 커밋 해시가 "이 예측이 그때 존재했다"의 증명이다.**
> 등록 대상은 shortlist가 아니라 **전체 랭킹**이어야 한다 — 어떤 분자든 새로 측정되면
> 검증 데이터가 되고, 랭킹 전체의 enrichment를 잴 수 있다.

---

## 3. 확정된 설계 결정

| # | 결정 | 근거 |
|---|---|---|
| **D1** | **E2E의 앞쪽 끝 = 적응증** | 목적함수의 부호를 적응증이 정한다. 자가면역 ↔ MPN에서 JAK2의 역할이 뒤집힌다 |
| **D2** | **적응증을 파라미터화** — 같은 패널이 여러 프로그램 | 게이트·데이터·회귀모델이 방향 무관(게이트 양성 = "any member"). gap 파생물만 각자 |
| **D3** | **Program은 목적함수 부호만 결정.** liability는 범위 밖 | hERG를 게이트 음성에서 빼면 G0 위반 |
| **D4** | **GPU는 도킹 + chemprop 둘. 생성은 조건부** | 각각 사전 게이트가 정의된 것만 |
| **D5** | **도킹 = 직교 바인더 증거. 선택성 랭킹 금지** | 선택성 신호 1.4 kcal/mol < 도킹 오차 1–2. 결합 여부 3+ kcal/mol > 오차 |
| **D6** | **카이랄성은 Stage A 층으로.** 배포 지문 불변 | 2단계 손실 0.5 % 미만(STATE §3b), 라이브러리 13.4 %가 미지정(§3c) |
| **D7** | **first-in-class는 LOTO 측정만 먼저.** PCM은 데이터 축적 후 | 타깃 7개로는 PCM 불가(STATE §9). 파라로그 전이 베이스라인부터 |
| **D8** | **탐색 절차 1~4단계는 스크립트, 5단계는 대화 + 서명** | 역방향 배선 검사가 테스트가 되려면 스크립트여야 한다 |
| **D9** | **케이스 DB = 커밋된 파일.** 새 인프라 없음 | git이 버전관리·백업·동기화·타임스탬프를 준다. 호스팅 DB가 사라지면 검증가능성도 사라진다 |
| **D10** | **재현 경로는 공개 무료 API만.** 상용 커넥터는 탐색 편의 | 카드에는 PMID/DOI/NCT를 적어 누구나 무료로 확인 가능하게 |
| **D11** | **414개 영구 봉인.** 학습 데이터는 릴리스 증분이 공급 | 희소한 것을 지키고 갱신되는 것을 쓴다 |
| **D12** | **Claude Code가 유일한 git writer** | Colab에 토큰을 넣지 않는다 |

### D2가 낳는 것 — 배선 검사

같은 데이터, 반대 목적함수, 양쪽 다 승인약으로 채점 가능하다.

| 프로그램 | 목적함수 | 검사 |
|---|---|---|
| 자가면역 (RA, 아토피) | `max pJAK1 − pJAK2` | **우파다시티닙 순위: 자가면역 > MPN** |
| MPN (골수섬유증) | `max pJAK2 − pJAK1` | **페드라티닙 순위: MPN > 자가면역** |
| | | 룩솔리티닙(이중): **양쪽에서 중간** |

**절대 순위가 아니라 상대 비교로 정의한다** — 룩솔리티닙은 JAK1/2 이중이라 어느 한쪽에서
1등일 이유가 없다. 상대 비교는 이중 억제제를 예외가 아니라 검사 항목으로 만든다.

> ⚠️ **표기 제약:** 이 승인약들은 학습셋에 있을 것이므로 **랭킹 성능 검증이 아니라
> "적응증 → 부호 → 랭킹" 배선 검사**로 표기한다. 그 이상을 주장하면 v1의 실수다.
> 승인약을 학습셋에서 뺀 hold-out 변형은 나중에 진짜 예측 테스트로 승격 가능하다.

### 환경 역할 분담

| 환경 | 역할 | git writer |
|---|---|---|
| **웹 플랫폼** | 읽기 + 케이스 선택 + 컨트랙트 내보내기 | ❌ (배포 제약상 쓰기 불가) |
| **Colab** | GPU 계산 → 결과 **파일** | ❌ |
| **Claude Code** | 오케스트레이션 + 분석 + **배치 승인** + 커밋 | ✅ **유일** |

**연결성 규칙:** 어떤 GPU 수치도 "노트북 안에서만" 존재해서는 안 된다. 산출물은 셋 중
하나여야 한다 — **커밋된 자산** / **컨트랙트 필드** / **커밋된 리포트**. 지키지 않으면
STATE §2의 91.3 %가 VALIDATION.md에 못 올라간 상황이 반복된다.

### 재현성의 두 기준 (Stage 0을 대화형으로 두기 위해)

| | 적용 | 기준 |
|---|---|---|
| **수치 재현성** | Stage 0.5 ~ 7의 모든 숫자 | **같은 스크립트 → 같은 값.** 오프라인 |
| **주장 추적가능성** | Stage 0의 Program 카드 | **각 주장에 출처·접근일. 독자가 확인 가능.** 같은 대화의 재현이 아님 |

두 번째는 과학 문헌의 재현성 개념과 같다. 이 구분을 명시하지 않으면 대화형 Stage 0이
규범 위반처럼 보인다.

---

## 4. 순서

```
 ─── 완료 (2026-08-03) ───────────────────────────────────────────
 ✅  죽은 import 2줄 수리                             STATE §1a
 ✅  scripts/chirality_audit.py                       STATE §3

 ─── 1단계: 측정 먼저 (값싸고, GPU 불필요, 로드맵을 바꿀 수 있음) ───
 N7   학습 곡선 측정 (축별)          ← ④가 성립하는지. 채널 1의 운명을 정한다
 N1   Tanimoto 최근접이웃 베이스라인  ← "유사도 검색 이상인가"
 0'   funnel_falsification_audit.py  ← 91.3 %를 VALIDATION으로 승격
 N14  early_stopping 아이소몬별 실제 동작 확인 (5분)
 D7   LOTO 축소판 (타깃 7개)         ← 파라로그 전이 베이스라인의 강도

 ─── 2단계: 기반 ────────────────────────────────────────────────
 0.5  적합성 스크린                  ← 생산 경로 수리를 강제·검증. CI 스모크 포함
 0    PROGRAM (적응증) + 배선 검사    ← D1·D2. 격차 7a/7b/7c 기록
 A1   potency 표기를 조건부 주장으로
 F    학습데이터 주입 seam (D0)       ← A2·B·G-2·N7 넷의 전제
 E    Run History 상태기계 + 라벨 원장 + 인덱스 경쟁 수리
 N11  평가 분할 라운드 0 봉인         ← 나중에 정하면 이미 돌린 라운드를 못 쓴다

 ─── 3단계: 모델 ───────────────────────────────────────────────
 G-1  도킹: 414 채점            [GPU]  ← 통과해야 랭킹 진입. 병행 가능(선행조건 없음)
 N21  Stage A 카이랄 정밀화 층 + 카이랄 AD
 N22  입체이성질체 열거 + 스프레드 보고
 B    게이트 재설계 (측정 음성 투입, 414 봉인)
 A2   2부 모델 + EV 재구성 (A3·C 흡수)
 N18  카이랄 A/B 실험 → Stage A 모델 채택 여부
 P4.1 assay-matched 라벨  /  P4.3 라이브러리 누출 (패널 인자화)

 ─── 4단계: 루프 ───────────────────────────────────────────────
 G    컨트랙트 스키마 1.2 (P4.4 흡수)
 D    오라클 2종 + N8 채널 대응 + N4 answer_status
 H    Analyze: 정지 규칙 + 재학습 트리거 + N10 게이트 임계 기록
 N2   채널 3 — 라운드 결과로 후보 생성 재정렬 (D를 상수에서 해방)
 N6   획득 점수 ÷ 비용 (구매가능성 연동)
 I    앱 읽기 전용 Run History + 케이스 브라우저
 N13  참신함 지표 4종 + shortlist 옆 표기

 ─── 5단계: 확장 ───────────────────────────────────────────────
 Stage 7  예측 사전등록 + 릴리스 증분 채점
 G-2  chemprop 멀티태스크        [GPU]  ← A2 이후에만
 Phase 3  획득함수 벤치마크 (TimeSplitOracle 위에서)
 G-3  생성 모델                  [GPU]  ← G-1 통과 시에만 열림
 PCM  proteochemometrics              ← 키나아제 대량 인제스트 후
```

### 순서의 근거 세 개

**① 왜 "측정 먼저"인가.** DMTA_DESIGN §1이 존재하지 않는 모집단 위에 설계를 세울 뻔했다.
같은 실수를 한 층 위에서 반복하지 않는 방법은 **설계 전에 세는 것**이다. N7·N1·D7이
전부 그 형태이고, 셋 다 GPU도 오라클도 필요 없다.

**② 왜 적합성 스크린(0.5)이 앞인가.** 그것이 세는 것 — 교차측정, 검열 레코드,
`library_molecule_overlap`, 라이브러리 재구축 — 이 STATE §1a가 부러졌다고 측정한 경로를
정확히 전부 통과한다. **만들려면 고칠 수밖에 없고, 고치면 그게 재발 방지 스모크가 된다.**
수리가 부채 갚기가 아니라 새 기능의 전제가 된다.

**③ 왜 G-1(도킹)이 3단계인데 병행 가능인가.** 선행조건이 없다. 414 목록을 파일로 봉인하면
(G6가 이미 요구) 도킹은 그 파일만 읽으면 되고 라이브러리 재구축과 무관하다. **첫 GPU
실험을 앞당기면서 순서 논리를 깨지 않는다.**

### 학습 곡선(N7)이 무엇을 결정하는가

L2(루프 학습)가 성립하려면 5단계 사슬이 필요하다: ① 라벨 도착 → ② 저장 → ③ 재학습이 읽음
→ **④ 모델이 유의하게 바뀜** → ⑤ 선택이 바뀜. **①②③은 설계돼 있고 ④는 설계도 측정도 없다.**

숫자를 놓으면 의심할 근거가 있다 — 학습셋 2,096(2020컷)에 라운드당 배치 70개는 **3.3 % 증가**다.

| 곡선 | 결론 |
|---|---|
| **이미 평평** | 채널 1(라벨 피드백) **강등.** 루프의 가치는 재학습이 아니라 **획득**에 있다 |
| **아직 가파름** | 재학습이 핵심. 배치 크기 재검토 |

**축마다 다를 가능성이 높다:** 회귀는 포화(2,096개는 ECFP4+GBM에 적지 않다), 게이트는
미포화(현재 음성이 전부 "부재로 추정"인데 **측정된** 음성이 처음 들어온다). 그렇다면 결론은
**"재학습은 게이트에만 값을 한다"** 이고, 이는 오라클 설계와 맞물린다(N8).

---

## 5. 항목 목록 (N1–N22)

| # | 항목 | 크기 | 단계 |
|---|---|---|---|
| **N1** | **Tanimoto 최근접이웃 베이스라인** — 모델이 유사도 검색을 이기는가 | 작음 | 1 |
| N2 | 채널 3 — 라운드 결과로 후보 생성 재정렬 | 중간 | 4 |
| N3 | 배치 승인 지점을 Claude Code 세션으로 명시 | 문서 | 4 |
| N4 | 라벨 원장에 `answer_status` (answered/no_answer/failed) | 작음 | 4 |
| N5 | 사전등록 예측에 `model_id` 필수 + 재점수화 정책 명시 | 작음 | 5 |
| N6 | 획득 점수 ÷ 비용 (구매가능성 연동) | 중간 | 4 |
| **N7** | **학습 곡선 측정 (축별)** — ④가 성립하는지 | 작음 | **1** |
| N8 | 오라클 ↔ 학습 채널 대응 명시 (검열은 게이트만) | 문서 | 4 |
| N9 | 보정 전용 무작위 스트림을 배포 캠페인에도 | 작음 | 4 |
| N10 | 게이트 임계 이동을 `AnalyzeMetrics`에 기록 | 작음 | 4 |
| **N11** | **평가 분할 라운드 0 봉인** | 작음 | **2** |
| N12 | "무엇이 학습하고 무엇이 안 하는가" 명시 | 문서 | 4 |
| N13 | 참신함 지표 4종 + shortlist 옆 표기 | 중간 | 4 |
| N14 | `early_stopping='auto'` 아이소폼별 실제 동작 확인 | 아주 작음 | 1 |
| N15 | 하이퍼파라미터 정책 명시 (튜닝 안 함을 **선택**으로) | 문서 | 2 |
| N16 | 튜닝 프로토콜 사전 고정 (§7) | 문서 | 2 |
| N17 | README에 **"판단 원리"** 절 신설 (STATE §4 요약 + 에탄올 예시) | 문서 | 2 |
| N18 | 카이랄 A/B 실험 → Stage A 모델 채택 여부 | 중간 | 3 |
| N19 | AD의 카이랄성 눈멂 문서화 | 문서 | 2 |
| ✅ N20 | 카이랄성 측정 재현 스크립트 | — | **완료** |
| N21 | Stage A 카이랄 정밀화 층 + 카이랄 AD | 중간 | 3 |
| N22 | 입체이성질체 열거 + 스프레드 보고 (shortlist 한정) | 작음 | 3 |

### 참신함 지표 (N13) — P3의 유일한 방어

과적합 방지와 참신함은 **방향이 반대**다. 정규화는 모델을 덜 확신하게(보수적으로) 만들고,
참신함은 모르는 영역으로 나가는 것(위험 감수)이다. **dropout류는 P3를 고칠 수 없다.**

> **AD는 참신함의 방어가 아니라 참신함의 적이다.** 그것은 AD의 잘못이 아니다 — AD의 일은
> "이 예측을 믿지 마라"이지 "이 분자를 찾지 마라"가 아니다. **하드 필터로 쓰면 그 구분이
> 사라지고 탐험이 구조적으로 0이 된다.**

따라서 **AD를 하드 필터에서 획득 페널티로 바꾸는 것**(구 IMPROVEMENT_PLAN §3C)은 "비교해
보자"가 아니라 P3의 유일한 구조적 방어이며, 우선순위가 올라간다.

지표: ① shortlist Murcko 스캐폴드 중 학습셋에 없는 비율 ② 학습셋 최근접 이웃까지 Tanimoto
거리 분포 ③ **라운드 간 신규 스캐폴드 비율**(떨어지면 collapse 진행 중) ④ 배치 내 다양성.

**보고 규칙: 참신함과 신뢰도를 나란히 놓는다. 하나로 합치면 v1의 복합 점수 실패를 반복한다.**

---

## 6. Stage 0 — 타깃/anti-타깃 탐색 절차

### 원리적 경계

| 전략 | 이 파이프라인 | 왜 |
|---|---|---|
| **선례 추종** (약이 있는 타깃) | ✅ 가능 | ChEMBL에 활성 데이터가 쌓여 있다 |
| **first-in-class** (약이 없음) | ❌ 불가 | **활성 데이터 없이 QSAR를 학습할 수 없다** |

> **이 탐색의 정직한 이름은 "새 타깃 발굴"이 아니라 "데이터가 충분한 타깃 중 선택성 문제가
> 실제로 존재하는 것 찾기"다.**

부수 이득: 선례가 있으면 **anti-target 근거도 확실하다**(부작용 라벨이 존재). 두 제약이
같은 방향을 가리킨다.

### 5단계 깔때기

```
[입력] 질병 A (사람이 정함)
  ├ 1. 열거      [자동] Open Targets(유전 근거) / ChEMBL(승인약→기전) / ClinicalTrials
  ├ 2. 선례 필터 [자동] 승인약 또는 임상 진입 이력이 있는가?     ★ 대부분 여기서 탈락
  ├ 3. 패밀리 필터[자동] 근연 파라로그 ≥3개인가?  없으면 Mode 1의 일
  ├ 4. 적합성    [자동] 교차측정 / 검열 모집단 / 누출  ← Stage 0.5
  ├ 5. 근거 수집 [반자동 → 사람 판정]
  └ [서명] Program 카드 커밋
```

**기각 사유도 케이스 DB에 기록한다** — "왜 이건 안 했나"가 나중에 자산이다.
**순환은 없다:** 적합성 스크린은 계산 가능한 것만 보고 안전성 근거는 보지 않는다.

### anti-target 근거의 강도

| # | 근거 | 강도 |
|---|---|---|
| 1 | **승인약의 부작용 라벨** | **최강 — 사람에서 관찰됨** |
| 2 | 기전 기반 (JAK2 → EPO/TPO) | 강함 |
| 3 | 유전학 — LoF 표현형 (gnomAD, OMIM/ClinVar) | 강함 |
| 4 | 구조 유사성 (같은 패밀리) | **약함 — "붙을 수 있다"이지 "나쁘다"가 아님** |

> **anti-target 등록에는 1~3 중 최소 하나를 요구한다. 4번만으로는 등록되지 않고, 대신
> "구조적 교차반응 감시" 칸에 들어간다.**
>
> STATE §7의 격차 7b·7c는 별개 실수가 아니라 같은 원인이다 — **anti-target을 "구조가
> 비슷하니까"로 정하고 안전성 근거를 묻지 않은 것.**

### 절차 자체의 검증 — 역방향 배선 검사

**"자가면역"을 입력으로 돌렸을 때 JAK 패널이 나오는가?** 4단계는 이미 측정된 값
(교차측정 3,624 / 검열 491)과 **정확히 일치해야 한다** — 계산 층의 단위 테스트가 된다.
그리고 5단계가 현재 패널과 **다른 답**(TYK2 포함, JAK3 감시)을 내놓는다면, 그것이 절차가
실제로 일한다는 증거다. **절차가 지금 패널을 그대로 승인하면 오히려 의심해야 한다.**

### 자동화의 경계

1~4는 **자동**(결정적·재현 가능), 5는 **반자동**(수집은 도구, 해석은 사람),
**판정은 언제나 사람 + 서명.** **LLM이 타깃에 점수를 매겨 랭킹하는 것은 하지 않는다** —
검증할 방법이 없다.

---

## 7. GPU / Colab

**기준: GPU는 "CPU로 못 하는 것"에만 붙는다. "더 빠르게"는 논거가 아니다.**

| # | 위치 | GPU가 필요한 진짜 이유 | 선행조건 |
|---|---|---|---|
| **G-1** | Stage 5 도킹 | **gnina의 CNN 리스코어링이 GPU 네이티브** | 없음 — 병행 가능 |
| **G-2** | Stage 2 chemprop 멀티태스크 | D-MPNN 학습은 CPU로 비현실적 | **F(주입 seam)** + A2 |
| **G-3** | Stage 3 생성 모델 | 파인튜닝 | **G-1이 414 게이트 통과** |
| (부수) | Stage 0 구조 | **AlphaFold DB에 이미 계산되어 있음** — ColabFold 불필요 | 새 패널 때만 |

### GPU가 잇는 것은 속도가 아니라 **끊긴 연결**

| 끊긴 연결 | 잇는 것 |
|---|---|
| **Design → Test** (B4: 제안한 모델이 채점) | **G-1.** 생성 모델만 GPU로 돌리면 **B4가 더 빨라질 뿐이다** |
| **Stage 0 → Stage 2** (새 타깃 = 자산 12종) | 구조 + 도킹이면 ChEMBL 자산 0으로 1차 점수 |
| **Stage 2 내부** (저데이터 아이소폼) | **G-2.** PIK3CB R² 0.582 |

### 도킹의 게이트와 금지

- **게이트:** 봉인된 414개에서 도킹 기반 판정이 현행 게이트(위양성 36 %)보다 낮은
  위양성률을, **보유 활성의 위음성률 악화 없이** 달성한다
- **반증조건:** 달성 못 하면 랭킹에서 빠지고 **포즈/접촉 그림 전용으로 강등.**
  그 결과도 VALIDATION.md에 올린다 (README 격차 #3의 정량적 확인)
- **금지:** 도킹 점수를 gap 계산이나 선택성 랭킹에 넣지 않는다 (D5)
- **자기충족 방지 (G8):** 도킹 설정(박스·스코어링 함수·프로토네이션·리셉터)을
  **414를 보기 전에 고정하고 커밋한다.** 설정 결정에는 라이브러리 밖 데이터를 쓴다

### GPU를 쓰지 않을 곳

10⁶ 라이브러리 스크리닝(ECFP+GBM은 백만 분자도 CPU 몇 분) · 배포 앱(영구히 CPU) ·
FEP/MD(쌍당 수 시간, 무료 티어와 불일치).

### 무료 티어 (T4) — 전부 한 세션에 들어간다

414 반증 도킹 ≈ 1,242회 → **한 세션** · chemprop ~10k 분자 → 수십 분 ·
생성 모델 파인튜닝 → 수 시간(체크포인트 필수) · ColabFold 1개 → 10~30분.

**제약은 성능이 아니라 세션 종료와 디스크 소실이다.** 그래서 연결성 규칙(§3)이 성능
문제가 아니라 정확성 문제다.

### 무료 환경 감사

CRO 어세이 하나를 빼면 **전부 무료다.** Streamlit Community Cloud / Colab 무료 T4 /
Vina·smina·gnina(오픈소스) / AlphaFold DB / PDB / ChEMBL / PubChem / Open Targets /
PubMed / ClinicalTrials / ZINC20 / Enamine 카탈로그.

---

## 8. 게이트

| # | 게이트 |
|---|---|
| **G0** | JAK 배포 자산 불변 — `model_id`, `gap_distribution` provenance, 컨트랙트 검증 동일 |
| **G1** | 주입 seam: 명시 주입 결과 == 캐시 경로 결과 (동일 `model_id`) |
| **G2** | 오라클: `train_keys` 조회 시 반드시 `None` (누출 차단) |
| **G3** | 오라클: `population()` 보고, 0에 가까우면 비활성화 / 소진 시 자동 종료 |
| **G4** | 재개: 라운드 3 중단 후 재개 시 라운드 4 동일 |
| **G5** | 개선·미개선을 **동일 비중**으로 보고 |
| **G6** | **봉인 유지**: 라이브러리 내 414개는 어떤 학습에도, **어떤 튜닝에도** 들어가지 않는다 |
| **G7** | 반증률이 매 라운드 자동 계산되어 `TestMetrics`에 남는다 |
| **G8** | **도킹 설정은 414를 보기 전에 커밋된다** (설정 파일 커밋 시각 < 채점 시각) |
| **G9** | **평가 분할은 라운드 0에서 봉인된다** — 학습셋만 자란다 |
| **G10** | **anti-target 등록에는 근거 1~3 중 최소 하나가 필요하다** (구조 유사성만으로는 불가) |
| **G11** | **생산 경로 스모크가 CI에 있다** — STATE §1a가 재발하면 실패한다 |

### 튜닝 프로토콜 (N16) — 착수 전에 고정

```
① 중첩 교차검증 또는 별도 검증 분할에서만. 테스트 분할은 절대 안 본다
② 봉인 414개는 튜닝에 절대 사용 금지 (G6)
③ 평가 분할은 라운드 0에서 봉인된 것을 쓴다 (G9)
④ 튜닝 전후를 둘 다 보고한다 (G5)
⑤ 목적함수를 튜닝 시작 전에 문서에 적는다
```

**튜닝으로는 91.3 %가 고쳐지지 않는다** — 원인은 하이퍼파라미터가 아니라 학습셋에 비활성이
하나도 없는 것이다. **그리고 "무엇을 최적화할 것인가"가 하이퍼파라미터를 고르는 것보다
먼저다** — R² 최적화는 위험하다(91.3 %는 R²가 좋은 상태에서 일어났다).

**따라서 순서는:** 지금은 튜닝하지 않고 **비일관성만 고친다**(N14 early_stopping 명시,
N15 L2=0을 의도로 기록). 튜닝은 **A2 이후**에 연다.

---

## 9. 범위 밖 (선언)

이 프로젝트는 **히트 발굴 ~ 히트투리드**를, **선택성** 목적으로, **공개 데이터**로,
**CPU 중심**으로 다룬다. Stage 0을 추가해도 "E2E 신약개발"이 되지 않는다 —
**"히트 발굴~히트투리드 구간의 E2E, 이제 타깃 근거까지 포함"**이 된다.

밖: 타깃 선정의 유전적 검증 자체 · **ADMET/DMPK 깊이**(hERG는 게이트 음성일 뿐 liability로
모델링되지 않는다 — 진짜이고 값싼 격차) · **합성/역합성** · IP/FTO · in vivo PK/PD · 규제.

**결합은 약이 아니다:**
```
생화학적 결합 → 세포 활성 → 선택성 → PK → 동물 효능 → 안전성 → 임상
    ↑                          ↑
    이 파이프라인이 다루는 것은 이 둘뿐이다
```
`pchembl 6` = IC50 **1 µM**이다. 승인약은 대개 nM(pchembl 8~9)이다. **이 저장소의 potency
floor는 약물 수준 기준이 아니라 스크리닝 컷이며, 문서와 UI가 그렇게 표기해야 한다.**

---

## 10. 케이스 데이터베이스

```
data/registry/           gitignored 유지 — 진행 중 라운드, 임시 캐시
assets/cases/            ★ 신설, 커밋 ★
  ├─ cases.jsonl                     케이스 색인 (사람이 읽음, git diff로 보임)
  └─ <case_id>/
       ├─ program.md                 적응증·타깃·근거·출처       ← "A를 목적으로"
       ├─ panel.json                 단백질 집합 + 적합성 결과    ← "B, C 타깃"
       ├─ rounds.jsonl               라운드 이력 + 반증률
       ├─ prediction_<date>.parquet  사전등록 랭킹 (+해시, +model_id)
       └─ outcomes.parquet           채점 결과                   ← "결과 K, J"
CASES.md                 ★ 자동 생성 ★ — 색인에서 만든 요약표. 손으로 쓰지 않는다
```

**커밋 정책 변경이 필요하다.** 현재 `.gitignore`가 `data/registry/`를 "runtime state, per
machine"으로 제외한다(STATE §8). **완료된 케이스만 `assets/cases/`로 승격**하고 진행 중인
것은 `data/`에 둔다. **Postgres는 협업자/CRO가 쓰기 시작할 때** — 그 트리거는 아직이다.

### 라벨 생애주기

```
새 측정 도착 → ① 등록된 예측에 대해 채점 (이 순간만 가능)
            → ② eligible_for_training = True 로 승격
            → ③ 다음 재학습이 읽음
```

**한 측정은 평가에 쓰이거나 학습에 쓰이거나 하나다.** 학습 파이프라인은
`eligible_for_training = True` 행만 읽고, 이를 테스트로 고정한다(G6의 일반화).

---

## 11. 서브에이전트 지침 (`.claude/agents/`)

**저작 시점에만 돈다. 실행 시점(CI)에는 절대 LLM을 호출하지 않는다.**

| 에이전트 | 역할 | 박아 넣을 규범 |
|---|---|---|
| `evidence-gatherer` | Stage 0 근거 수집 | **모든 주장에 출처+접근일. 판정 금지** |
| `red-team` | 반증자 — "왜 하면 안 되는가" | 실패 임상·안전성 시그널·경쟁 포화 우선 |
| `measurement-runner` | 실험 실행 | **산출물은 스크립트와 그 출력. 에이전트 요약을 결과로 받지 않는다** |
| `panel-bootstrapper` | 새 패널 추가 | 적합성 → 누출 검사 → rationale 없이는 등록 금지 |

`measurement-runner`의 규범이 특히 중요하다 — **91.3 %가 VALIDATION.md에 못 올라간 이유가
정확히 "재현 스크립트 없이 측정만 됐다"이므로, 그 실수를 자동화해서는 안 된다.**

**이번 작업에서는 사용하지 않는다.** JAK 근거는 이미 충분히 나와 있고, 서브에이전트는
**모르는 패밀리를 추가할 때** 값이 난다.

---

## 12. 결정이 필요한 것 — 답을 강요하지 않고 측정 계획과 짝짓는다

| # | 열린 질문 | 무엇을 측정하면 정해지는가 |
|---|---|---|
| 1 | A2를 2부 모델로 갈 것인가, 검열 손실 회귀(Tobit/AFT)로 갈 것인가 | 2부 모델 후 414 반증률. 안 내려가면 표현이 범인 |
| 2 | 시간 컷 2018 vs 2020 | 두 컷 모두 돌려 오라클 크기 ↔ 모델 강도 트레이드를 본다 |
| 3 | P4.1 매칭: same-document(5,049) vs same-assay(2,820) | 둘 다 측정 후 Spearman을 pooled 0.798 / Ki-Kd 0.462 옆에 놓는다 |
| 4 | 재학습 시 이전 라운드를 재점수화하는가 | **권고: 하지 않되 명시 표기.** 각 라운드는 그 시점 모델의 판단 |
| 5 | 카이랄 모델이 achiral보다 나은가 | **N18.** 안 나으면 Stage A는 열거 기능만 남긴다 |
| 6 | 릴리스당 라이브러리 분자 몇 개가 새 JAK 측정을 얻는가 | 과거 릴리스 간 증분을 세어 사전등록의 검정력을 본다. 작으면 장기 적립 항목 |
| 7 | 적합성 스크린의 후보 패밀리 범위 | JAK·PI3K 외 CDK·PDE·HDAC 등 — 스크린을 돌려 정한다 |

**답을 지금 정하면 그것이 다음 문서가 반증할 대상이 된다.** 열린 질문과 측정 계획을 짝지어
두는 편이 낫다.

---

## 13. 리스크

| 리스크 | 완화 |
|---|---|
| 414개를 실수로 학습/튜닝에 넣어 평가 기반을 잃는다 | G6를 테스트로 고정. 봉인 목록을 파일로 커밋 |
| 도킹 설정을 414 성적을 보며 고른다 | **G8** — 설정 커밋이 채점보다 앞선다 |
| 재학습이 배포 컨트랙트를 조용히 깬다 | 캠페인 사설 네임스페이스 + G0 |
| 참신함이 조용히 소멸한다 | **N13 지표 ③(라운드 간 신규 스캐폴드 비율)** — 다른 어떤 지표에도 안 나타난다 |
| Stage 0 추가가 과대주장으로 읽힌다 | §9의 선언을 README 최상단에 유지 |
| 커넥터가 유료화·중단된다 | **D10** — 카드에 원 출처를 적어 무료로 확인 가능하게 |
| 카이랄 재채점을 "직교 증거"로 표기한다 | 도킹만 직교(다른 정보원). 카이랄은 **더 높은 해상도**(같은 정보원) |
| PCM을 베이스라인 없이 도입한다 | **D7** — 파라로그 전이(코드 몇 줄)를 반드시 먼저 |
