# STATE — 측정된 것과 반증된 것

**이 문서의 규칙: 측정만 적는다. 계획은 [ROADMAP.md](ROADMAP.md)의 일이다.**

모든 수치는 커밋된 자산(`assets/`)과 이 저장소의 코드로 얻었다. 각 항목에 재현
명령이 붙어 있고, **재현 스크립트가 없는 수치는 그렇다고 표기한다** — 그 표기 자체가
할 일 목록이다.

마지막 갱신: 2026-08-09 · 데이터 릴리스: ChEMBL_37

---

## 0. 한 문장 요약

> **선택성 축은 검증됐고(Spearman 0.798), 결합 여부 축은 실측 비결합자에서 93.0 % 반증됐다.
> 자산을 소비하는 계층은 건강하고, 자산을 생산하는 계층은 최근까지 두 곳이 부러져 있었다.
> 그리고 두 축 모두 데이터 양에는 이미 포화했다(§4b) — 남은 것은 양이 아니라 라벨의 정의다.
> 루프는 3라운드 돌았고(§11), 늘어난 것은 획득의 실력이 아니라 풀의 고갈이다.
> 게이트를 측정 음성으로 고치려는 시도는 반증됐다(§2b) — 라벨이 아니라 표현의 한계다.**

### 0a. DMTA 준비도 — 축별로 무엇이 있고 무엇이 막혀 있는가

**"DMTA에 도달"을 취향 판단이 아니라 네 행의 상태로 만든다.** 각 Wave 종료 시 갱신한다.

| 축 | 있는 것 | 남은 것 | 상태 |
|---|---|---|---|
| **D** esign | `funnel.screen_library` · `dmta.score` · 라이브러리 38,592 | potency 축 93.0 % 반증(§2) — **2부 모델이 같은 재적합 위에서 AUC 0.890→0.956으로 고침(§2c), 미배포.** gap 축은 시간 이동에서만 방어됨(§4c)이고 2부 모델이 gap을 `P · gap`으로 재정렬하는 효과는 **미측정** | **작동 · 주장 제한적** |
| **M** ake | Ro5+PAINS(라이브러리), SA 점수 | SA는 `generate.py`(채널 ③)에만. **구매가능성 없음** — ROADMAP이 기여의 원천으로 지목한 채널 ②에 MAKE 단계가 통째로 없다 | **부분** |
| **T** est | `TimeSplitOracle` 1,078 · 봉인 비결합자 414(평가 전용) | 오라클 1종. **게이트는 측정 음성으로 못 고침(§2b) — 표현 한계이므로 직교 증거(G-1 도킹)가 필요** | **작동 · 게이트 미해결** |
| **A** nalyze | 반증률(G7) · 정지 규칙 · 획득 페널티 · 라벨 원장 | 재학습 트리거는 **측정으로 무의미해짐**(§4b), 만들지 않음 | **작동** |

**루프는 닫혔다 — 단, 캠페인에서.** `app.py`가 서비스하는 배포 스크린은 여전히 배포 모델 +
와이드 라이브러리 단방향이다. §11의 3라운드는 봉인 eval fold 위에서 **동결 모델**로 돈
별개 트랙이며, 두 트랙은 아직 만나지 않는다.

---

## 1. 계층별 상태

| 층 | 내용 | 상태 |
|---|---|---|
| (a) 커밋된 자산 | `assets/jak/`, `assets/models/jak/`, `assets/library/`, `assets/evidence/` | 완전. 오프라인 부팅 가능 |
| (b) 자산을 **읽는** 코드 | `funnel`, `conformal`, `applicability`, `deep_dive`, `app.py` | 218 테스트 통과 |
| (c) 자산을 **만드는** 코드 | `data/library`, `data/negatives`, `scripts/*audit*`, `reproduce.sh` | **2026-08-03 수리됨** (아래) |

### 1a. 생산 경로 두 곳이 부러져 있었다 — 수리 완료

```
src/data/library.py:76      negatives.jak_positive_smiles  →  positive_smiles
scripts/gate0_audit.py:26   src.data.jak  →  src.data.panel_data + panels.get_panel
```

STEP 15의 패널 일반화 때 심볼이 개명됐고 호출부가 따라가지 않았다. **와이드 라이브러리
재구축과 Gate 0 감사가 둘 다 실행 불가능한 상태였고, 둘 다 `scripts/reproduce.sh`에
들어 있다.** 2026-08-03에 수리했다.

**176개 테스트가 이것을 못 잡은 이유는 구조다.** `tests/test_library.py:26`이 깨진 바로
그 심볼을 monkeypatch한다. 같은 패턴이 `tests/test_binder_gate.py:34`,
`tests/test_loop_integration.py:45-50`에 있다. 테스트가 대체하는 지점이 정확히 고장난
지점이므로, 진짜 생산 경로는 한 번도 실행되지 않았다. 이것은 테스트 위생 문제가 아니라
**주입 seam 부재의 다른 얼굴**이다 — 데이터를 인자로 못 받으니 모듈 전역을 갈아끼울 수밖에 없다.

2026-08-08에 `isoform_regressor.evaluate` / `train_and_cache`가 `data=` 를 받는다
(ROADMAP F). 주입 실행은 모델 캐시를 읽지도 쓰지도 않으므로 부분 학습된 모델이 배포
모델 자리에 앉을 수 없다. `src/selectivity.py`는 손대지 않았다 — `evaluate_split`이
이미 데이터와 분할을 인자로 받는다.

**재발 방지 장치 (2026-08-08, G11).** `tests/test_production_path.py`가 두 경로를
아무것도 monkeypatch하지 않고 커밋 자산 위에서 실행한다. CI가 이미 `pytest tests/`를
돌리므로 워크플로 변경은 없다. 측정으로 확인했다 — 두 개명을 되돌려 넣으면 새 스모크
2개가 실패하고, **`tests/test_library.py`는 그대로 7 passed**다.

> **덮지 못하는 것:** 네트워크 재구축(`use_cache=False`)의 fetch 루프. CI는 오프라인이고,
> 두 파손 모두 fetch 루프에 있지 않았다.

---

## 2. 배포된 퍼널의 첫 외부 반증

와이드 라이브러리 안에 **측정된 비결합자 414개**(드러그라이크)가 있다. pchembl이 없어
학습에서 구성상 제외됐고, 게이트 학습 음성과도 거의 겹치지 않는다(414개 중 7개, 1.7 %).
배포 모델에게는 처음 보는 분자이고, 정답이 있다.

```bash
python scripts/funnel_falsification_audit.py
```

| 관문 | 결과 |
|---|---|
| Tier 1 potency 예측 | pred JAK1 중앙값 **6.50** (실측 상한 중앙값 5.80 ≈ 2 µM인 분자들에게) |
| **반증률** | **385 / 414 = 93.0 %** — 초과폭 중앙값 **0.65 log** |
| Tier 0.5 바인더 게이트 | 149개 통과 (**36.0 %**) |
| Tier 1 potency floor | 334개 통과 (**80.7 %**) |
| Tier 2 in-domain | 221개 (53.4 %) |
| 캐스케이드 (게이트 → floor) | 127개 → **in-domain 76개** |

**이전 판의 91.3 %를 대체한다.** 이전 수치는 `pred_JAK1`을 분자의 상한과 비교했는데,
414개 중 **JAK1 검열 기록을 가진 것은 74개뿐**이고 나머지는 JAK2·JAK3에서만 검열됐다.
즉 JAK1 예측을 JAK2 측정과 비교한 페어링이었다. 스크립트는 아이소폼을 맞춰 비교한다
(744개 (분자, 아이소폼) 쌍). 정합 비교에서 **JAK1 쌍 74개는 74개 전부 반증**되며
초과폭 중앙값은 1.89 log다.

이전 판의 "Tier 2 도달 12개"도 대체한다. 그것은 라이브러리 gap 분포 상위 5 % 컷을 추가로
적용한 값이고, `screen_library`가 414개를 입력받으면 `tier1_keep=300`이 걸리지 않아
실제 코드 경로는 127개를 Tier 2로 보낸다.

**측정의 보수성:** 검열 기록의 상한은 10 µM 하나가 아니다(중앙값 2 µM, 29 %만 ≥10 µM).
분자마다 여러 기록이 있을 때 **가장 약한 주장**을 골랐으므로 93.0 %는 **하한**이다.

**말하는 것.** 회귀모델은 활성만 보고 학습했으므로 **음의 동적범위가 없다.**
`negatives.py` 독스트링이 같은 것을 다르게 적는다 — 도메인 밖에서 트리 앙상블은 학습
평균(~6.3)으로 회귀하므로 **에탄올이 760 nM JAK1 저해제로 점수가 나온다.**

**말하지 않는 것.** 전부 음성 샘플이므로 이것은 **위양성률이지 재현율이 아니다.**
놓친 활성에 대해서는 아무 말도 하지 않는다.

**해석.** 최종 산출물로 새는 양은 414 → 게이트·floor 통과 127 → in-domain 76이다. 결론은 "퍼널이 망가졌다"가
아니라 **"potency 축은 검증된 적 없고 크게 틀렸으며, selectivity + AD 캐스케이드가 그것을
가리고 있었다"**이다. 그리고 **AD는 값을 치렀다는 증거를 얻은 유일한 층이다.**

### 2a. 양성 팔 — floor는 활성을 죽이지 않는다 (2026-08-08)

음성 팔만으로는 해석이 불가능하다. 음성만 채점하면 **조이는 것이 언제나 개선으로 보인다.**
양성 팔은 2020년 컷 이전 데이터로 회귀모델을 재학습(주입 seam)한 뒤, 그 모델이 한 번도 못 본
2020년 이후 실측 활성을 채점한다.

| Tier 1 potency floor | 통과 |
|---|---:|
| 414 실측 비결합자 | **80.7 %** |
| **1,928 post-2020 실측 활성** (JAK1 pchembl ≥ 6) | **99.4 %** |
| | **floor가 버린 진짜 활성: 11개 (0.6 %)** |

**floor는 활성을 버리지 않는다.** 착수 전 가설("반증된 축이 분자를 삭제하고 있다")은
**반증됐다.** 다만 판별력도 거의 없다 — 결합하지 않는 것으로 측정된 분자의 **80.7 %를
통과시킨다.** 방향은 맞고(99.4 > 80.7) 폭은 18.7 포인트뿐이다.

> **따라서 floor는 제거 대상이 아니다.** 제거하면 활성 0.6 %를 되찾고 비결합자 19.3 %를
> 추가로 들인다. A1은 구조 변경이 아니라 표기 작업으로 남는다.

**양성 팔이 덮지 못하는 것:** Tier 0.5와 Tier 2. 정직한 재현율에는 게이트와 AD 레퍼런스도
같은 컷으로 재학습해야 하는데 둘 다 아직 주입 인자를 받지 않는다.

**부수 발견:** 커밋된 `assets/jak/*.parquet`에는 `year_first` 컬럼이 없다(`smi`, `pchembl`,
`n_meas`뿐). `_collapse`가 만들지만 번들에 들어가지 않았다. **`scripts/assay_time_audit.py`는
네트워크 재구축 없이는 돌지 않는다.** 이 감사는 대신 증거 저장소의 `document_year`를 쓴다
(매칭률 99.8 %).

### 2b. 게이트 음성 재정의 (B) — 반증됨 (2026-08-09)

```bash
python scripts/gate_ab_audit.py
```

먼저 배포 게이트를 더 넓은 검열 모집단에 걸어봤다. §2의 36 %는 낙관적이었다.

| 그룹 | n | 통과 (thr 0.544) | 중앙 P |
|---|---:|---:|---:|
| **측정 음성 (미학습)** | 1,720 | **65.3 %** | **0.921** |
| 봉인 414 | 414 | 36.0 % | 0.245 |
| 추정 음성 (학습) | 2,000 | 0.4 % | 0.007 |
| 양성 (학습) | 2,000 | 99.8 % | 0.999 |

414는 **와이드 라이브러리**에서 왔으므로 다른 타깃의 화학이다. 진짜 JAK 프로그램
비결합자에 대한 위양성률은 **65.3 %**다. 임계를 0.99까지 올려도 34.6 %가 통과하고 양성
14 %를 잃는다 — **임계 재보정은 반증됐다.**

### A/B — 음성 클래스만 다르게

양성(pre-2020 11,789) · 추정음성 · 지문 · 추정기 · 시드 · 임계 규칙 전부 동일. 후보만
`measured[train]` 1,290개를 추가로 받는다. 셋 다 양쪽 미학습.

| | BASELINE (추정음성만) | CANDIDATE (+측정음성) |
|---|---:|---:|
| Youden 임계 | 0.046 | 0.019 |
| measured[eval] 430 통과 | 74.4 % | 63.5 % |
| 봉인 414 통과 | 53.6 % | 64.0 % |
| presumed[eval] 2,428 통과 | 3.5 % | 10.1 % |
| **positives[eval] 3,433 유지** | **96.9 %** | 83.5 % |
| **ROC-AUC vs measured[eval]** | **0.741** | **0.735** |
| **ROC-AUC vs 봉인 414** | **0.925** | **0.773** |

**판정: 측정 음성 투입은 판별력을 전혀 개선하지 않았다.** 목표 모집단에서 AUC 0.741 →
0.735(차이 없음), 봉인 414에서 0.925 → **0.773으로 악화**.

동일 양성 재현율에서 비교하면 후보가 매 지점에서 더 나쁘다:

| 양성 유지 | BASELINE measured 통과 | CANDIDATE | delta |
|---:|---:|---:|---:|
| 99.0 % | 84.0 % | 98.4 % | **+14.4** |
| 95.0 % | 70.0 % | 91.2 % | **+21.2** |

> **고정 임계에서는 후보가 이긴다** (thr 0.9: 7.0 % vs 43.5 %). 그것은 후보의 확률 분포가
> 통째로 내려간 **보정 이동**일 뿐이고, 양성 중앙값도 0.996 → 0.781로 같이 내려갔다.
> matched-recall 통제와 AUC가 없었다면 이 실험은 승리로 보고됐을 것이다.

**왜 악화됐는가 (해석).** 봉인 414는 라이브러리 분자, 즉 **off-target 화학**이고 추정 음성과
같은 분포다 — 베이스라인이 0.925를 내는 이유다. 측정 음성 1,290개는 **JAK 프로그램 화학**이라
"JAK처럼 생겼는데 음성"을 가르치고, 그것이 off-target에 대해 갖고 있던 경계를 흐린다.

> **결론: 게이트의 실패는 라벨 클래스의 문제가 아니라 표현의 한계다.** JAK 논문의 측정
> 비결합자는 활성 유사체와 같은 ECFP4 이웃에 있다. §3a가 입체이성질체에서 측정한 것과
> 같은 구조의 라벨 잡음이, 이번엔 활성 경계를 가로질러 나타난다. **올바른 라벨을 더 넣어도
> 차이를 볼 수 없는 표현은 고쳐지지 않는다.**

**이것이 로드맵을 바꾼다.** 지문 경로로는 36 %(실제로는 65 %) 위양성을 못 내린다는 것이
측정됐으므로, **G-1 도킹의 값이 올라간다** — 로드맵에서 유일하게 직교하는 정보원이다(D5).

**한계:** 측정 음성 1,720 vs 추정 음성 12,144. 스캐폴드 분할 시드 1개. 가중 학습은
시험하지 않았다(§8이 A2까지 튜닝을 봉쇄).

**배포는 하지 않는다.** 후보가 졌고, 이겼더라도 배포 게이트 교체는 임계를 옮기고 Tier 0.5를
옮기고 배포 shortlist와 그 아래 VALIDATION 수치 전체를 옮긴다. G0 이벤트이며 별도 사안이다.

### 2c. 2부 모델 (A2) — 채택 (2026-08-10 측정)

```bash
python scripts/two_part_audit.py
```

Tier 1은 회귀기 출력만으로 랭킹한다. 회귀기는 정량화된 pchembl로만 학습했으므로 패널에
붙은 적 없는 분자에는 학습 평균을 돌려준다. 2부 모델은 원하는 값이 "붙는다면 얼마나
센가"가 아니라 "얼마나 셀 것인가"라고 말한다:

```
EV = P(binder) · reg + (1 − P(binder)) · FLOOR
```

**FLOOR는 봉인 414를 보기 전에 커밋됐다** (`src/models/two_part.py`, 상수에 적용한 G8).
5.0 = 10 µM, 4.0은 민감도 점검. 둘 다 보고하고 사이를 탐색하지 않는다.

**두 팔이 같은 모델을 쓴다.** 회귀기(JAK1 8,247 / JAK2 10,008 / JAK3 5,025)와 게이트
(양성 11,789 + 추정음성 12,144)를 전부 2020 컷 이전으로 재적합했다. §2의 감사는 음성 팔에
배포 모델, 양성 팔에 재적합 모델을 써서 비교가 불가능하다고 스스로 적어 뒀고, 여기서 닫힌다.
**따라서 아래 베이스라인은 §2의 93.0 %와 다른 수치다 — 섞어 읽지 않는다.**

봉인 414 vs 컷 이후 실측 활성 1,928.

| | BASELINE (회귀기만) | floor 5.0 | floor 4.0 |
|---|---:|---:|---:|
| **C. ROC-AUC** 활성 vs 봉인 414 | 0.890 | **0.956** | 0.955 |
| **B. 95 % 재현율서 봉인 통과** | 41.8 % | **19.6 %** | 23.2 % |
| A. 반증률 (참고) | 96.1 % | 37.7 % | 15.9 % |
| 봉인 JAK1 점수 중앙값 | 6.49 | 5.08 | 4.14 |
| 활성 JAK1 점수 중앙값 | 7.39 | 7.30 | 7.28 |

**사전등록 규칙(AUC +0.02 이상 **그리고** matched-recall 95 %에서 통과율 하락) 두 조건을
모두 충족 — 채택.** AUC +0.066, 통과율 −22.2 %p.

> **A를 판정에서 뺀 것이 이 측정의 핵심이다.** `P ≤ 1`이므로 EV는 모든 분자에서 회귀기
> 이하다. 96.1 % → 37.7 %는 판별력이 개선되지 않았어도 나오는 숫자이며, §2b가 한 웨이브
> 전에 잡은 보정 이동과 같은 구조다. B와 C만이 그 자유도를 제거한다.

**floor 5.0과 4.0의 AUC 차이가 0.001이다.** 판별력은 사전등록한 상수가 아니라 `P(binder)`
에서 나온다 — 상수를 어디에 두었든 결론이 같으므로 G8의 선택이 결과를 만들지 않았다.

**gap 축에는 새 정보가 들어오지 않는다.** 게이트가 패널당 하나라 `P`가 모든 아이소폼에서
같고, `EV(target) − max EV(off) = P · gap`이다.

> **정정.** 이 소절의 첫 판은 "gap 축은 구조적으로 불변"이라고 적었다. 틀렸다. 새 정보가
> 안 들어오는 것과 순위가 보존되는 것은 다르다 — `P`가 분자마다 다르므로 곱셈은 순서를
> 바꾼다(gap 2.0 / P 0.50 = 1.00이 gap 1.2 / P 0.95 = 1.14에 밀린다). 낮은 `P`를 강등하는
> 것이 의도지만, **그 강등이 선택성 축에서 이득인지는 이 감사가 재지 않았다.** 위 표는
> 전부 potency 축(target 아이소폼 점수) 수치다.

**§2b와의 관계.** 게이트의 판별력은 그대로다 — 바뀐 것은 그 확률을 **버리지 않고 potency에
곱한 것**뿐이다. §2b는 "게이트를 더 좋게 만들 수 없다"였고, 여기서 나온 것은 "이미 있는
게이트를 덜 낭비할 수 있다"이다. 두 결과는 모순되지 않는다.

**한계:** 스캐폴드 분할 없이 시간 컷 하나. 게이트는 추정 음성만으로 재적합(§2b가 측정 음성
투입을 반증). 튜닝 없음. 활성 정의는 `pchembl >= 6.0` 하나.

**배포는 하지 않는다.** Tier 1의 점수 정의를 바꾸는 것은 potency floor의 의미를 바꾸고
배포 shortlist와 그 아래 VALIDATION 수치 전체를 옮긴다. G0 이벤트이며 별도 사안이다.

---

## 3. 표현이 볼 수 없는 것 — 카이랄성 (2026-08-03 측정)

지문은 `GetMorganGenerator(radius=2, fpSize=2048)` — **`includeChirality` 미지정 = 꺼짐.**
README 격차 #7이 이것을 오래 알려진 한계로 적어 두었으나 **크기를 잰 적은 없었다.**

```bash
python scripts/chirality_audit.py
```

### 3a. 학습 데이터의 붕괴

| | JAK1 | JAK2 | JAK3 |
|---|---:|---:|---:|
| 학습 분자 | 10,468 | 12,680 | 7,457 |
| 스테레오센터 보유 | 49.5 % | 44.8 % | 48.4 % |
| **입체이성질체 충돌 그룹** | **824** | 811 | 408 |
| 영향받는 분자 | **1,848 (17.7 %)** | 1,844 | 927 |
| 그룹 내 pchembl 차이 중앙값 | 0.47 | 0.44 | 0.22 |
| p90 | 1.63 | 1.48 | 1.45 |
| **최대** | **3.53 log (≈3,400배)** | 3.08 | 3.09 |
| **≥1 log 차이 그룹** | **226** | 199 | 83 |
| 기타(해시) 충돌 그룹 | 14 | 39 | 11 |

**충돌의 98 %가 순수 카이랄성이다** — 해시 충돌이 아니다. 두 분자가 하나의 입력에 두 개의
라벨을 준다. **이것은 과적합이 아니라 구성상 주입된 라벨 잡음이며, 데이터가 늘어도 사라지지 않는다.**

**규모의 정직한 비교:** ≥1 log 그룹 226개 × 평균 2.2분자 ≈ 450분자에서 최소 0.5 log 오차가
보장된다 — 학습셋의 약 4 %. §2의 초과폭 중앙값 0.65 log와 비교하면 **진짜 결함이지만
93.0 %의 주범은 아니다.**

### 3b. 2단계 설계가 잃는 것 — 거의 없다

achiral 예측은 평균으로 **수축**한다. 수축은 활성을 아래로 당기지만 비활성을 위로도 당기고,
필터는 위로 통과시키므로 활성은 대부분 살아남는다. 측정:

| | 활성(≥6) | **풀링으로 floor 아래로 가라앉는 활성** |
|---|---:|---:|
| JAK1 | 9,618 | **50 (0.52 %)** |
| JAK2 | 11,000 | 31 (0.28 %) |
| JAK3 | 6,251 | 9 (0.14 %) |

> **achiral 지문은 나쁜 랭커이지만 좋은 필터다.** 손실이 0.5 % 미만이므로, 1차 스크리닝을
> achiral로 두고 정밀화를 뒤 단계로 미루는 설계가 측정으로 지지된다.

### 3c. 스크리닝 라이브러리의 입체 정보 (n = 38,594)

| | |
|---|---|
| 지정된 스테레오센터 보유 | **36.3 %** (14,009) |
| **미지정** 스테레오센터 보유 | **13.4 %** (5,188) |
| 그중 미지정 센터가 1개뿐 | **84 %** (4,361) → 가능한 이성질체 2개 |

**함의:** 카이랄 모델을 배포 스크리닝에 쓰면 **정보가 없는 분자 5,188개를 학습 분포 밖에서
채점**하게 된다(학습셋은 절반이 지정). 미지정 센터는 뒤 단계에서 **열거**할 대상이다.

### 3d. 덜 명백한 두 번째 영향 — AD가 카이랄성에 눈멀어 있다

Applicability Domain은 같은 지문으로 Tanimoto 최근접 이웃을 잰다. 입체이성질체는 **거리 0**이다.
따라서 **어떤 분자가 in-domain으로 판정되는 이유가 그것의 거울상이 학습셋에 있었기
때문일 수 있다.** §2에서 AD가 얻은 좋은 성적은 이 눈멂을 물려받는다.

**누출 검사는 안전하다** — `library_molecule_overlap`은 InChIKey(입체 층 포함)를 쓰고,
`standardize`는 `MolToSmiles` 기본값으로 입체를 보존한다. **데이터 계층은 카이랄성을 지키고
있고, 지문에서만 버려진다.**

---

## 4. 판단 원리 — 이 파이프라인이 무엇을 보고 무엇을 안 보는가

```
분자 → [원자마다 반지름 2까지 부분구조 열거 → 해시] → 2048비트
     → HistGradientBoosting (max_iter=400, lr=0.08, random_state=0)
     → pchembl
```

**판단 원리는 하나다: "이 분자의 부분구조 조각들이, 학습셋에서 활성이었던 분자들의
조각들과 닮았는가."**

**모르는 것:** 단백질(모델은 JAK1이 무엇인지 모른다 — "JAK1 라벨이 붙은 숫자들"을 봤다),
결합의 물리, 3D 형태, **카이랄성**(§3).

**선택성(gap)을 맞히는 방식:** 아이소폼마다 별도 모델이 같은 표현·다른 라벨로 학습된다.
따라서 잡는 것은 **문헌에 이미 발표된 선택성 SAR의 압축**이다. 새 물리가 아니다.
한계는 문헌에 없는 SAR을 못 찾는 것이고, 가치는 사람이 논문 수만 편의 패턴을 동시에
적용할 수 없다는 것이다.

**모든 층이 같은 2048비트에서 나온다** — 회귀, 게이트, AD, conformal. README 격차 #2가
"직교 증거가 아니라 같은 표현 공간의 신뢰도 주석"이라고 적은 것의 코드 수준 사실이다.

**하이퍼파라미터는 튜닝된 적이 없다.** `l2_regularization`은 기본값 0(꺼짐)이다. 배포 모델은
`random_state=0` 단일 시드이며 앙상블이 아니다. `SEEDS=(0,1,2,3,4)`는 평가에만 쓰인다.

> 튜닝하지 않은 것은 테스트셋 오염을 피한다는 점에서 방어 가능하지만, **"안 하기로 했다"와
> "안 해봤다"가 문서에서 구분되지 않는다.** early_stopping의 비대칭은 의도된 바 없다.

### 4a. early_stopping='auto'의 실제 동작 (N14, 2026-08-08 측정)

```bash
python scripts/early_stopping_audit.py
```

| 아이소폼 | 배포 n | 평가 학습 n | 켜짐? | `n_iter_` | 발동? |
|---|---:|---:|:---:|---:|:---:|
| JAK1 | 10,468 | 8,374 | **True** | 400 / 400 | **False** |
| JAK2 | 12,680 | 10,144 | True | 400 / 400 | **False** |
| JAK3 | 7,457 | 5,966 | False | 400 / 400 | **False** |

이전 판이 "정규화 동작이 다를 수 있다"고 적었다. **측정 결과 정규화 차이는 0이다** — 셋 다
`n_iter_ == max_iter`이므로 early stopping이 **한 번도 아무것도 멈추지 않았다.**

실제 비대칭은 **학습 데이터**에 있다. 켜진 쪽은 `validation_fraction=0.1`을 홀드아웃하고
거기에 절대 학습하지 않는다.

| | 실제 학습 행 |
|---|---:|
| JAK1 | 9,421 (10 % 버림) |
| JAK2 | 11,412 (10 % 버림) |
| JAK3 | **7,457 (전량)** |

**아무것도 멈추지 않은 기제를 위해 JAK1·JAK2가 각각 10 %를 냈다.**

두 번째 발견 — **JAK1은 임계를 사이에 두고 뒤집힌다.** 배포(10,468)는 켜지고 평가 학습
분할(8,374)은 꺼진다. **VALIDATION.md의 JAK1 수치는 배포된 것과 다른 설정의 모델에서
나왔다.** JAK2·JAK3은 양쪽이 같은 편에 있어 일관된다.

> **고치지 않았다.** `early_stopping`을 명시하면 세 모델이 재학습되고 `model_id`가
> 바뀌어 G0가 깨진다. 재학습을 수반하는 단계(A2)에서 함께 처리한다. 여기서는 표기만 한다.
> 손실의 크기는 N7(학습 곡선)이 정한다 — 곡선이 평평하면 10 %는 공짜고, 가파르면 실손실이다.

---

### 4b. 학습 곡선 — 두 축 모두 포화 (N7, 2026-08-08 측정)

```bash
python scripts/learning_curve_audit.py     # 약 12분
```

scaffold 분할을 고정하고, 학습 반쪽의 **중첩 접두부**로 재학습하며, 매번 **같은 test 반쪽**에
채점한다. 부분집합은 학습 반쪽 안에서 무작위이므로 이것이 재는 것은 **양의 가치**이지 잘 고른
배치의 가치가 아니다.

| 학습 n | JAK1 Spearman | JAK1 MAE | 게이트 ROC-AUC |
|---:|---:|---:|---:|
| 10 % | 0.7746 | 0.6080 | 0.9931 |
| 40 % | 0.8536 | 0.4882 | 0.9976 |
| 80 % | 0.8717 | 0.4541 | 0.9983 |
| 100 % | **0.8735** | **0.4507** | **0.9983** |

| 축 | 70분자 라운드당 변화 | 시드간 표준편차 | |
|---|---:|---:|---|
| 회귀 Spearman | +0.00008 | ±0.0072 | **90배 아래** |
| 회귀 MAE | −0.00014 | ±0.0147 | **100배 아래** |
| 게이트 ROC-AUC | +0.00000 | ±0.0006 | **구분 불가** |

**세 축 전부 노이즈 바닥 아래다. 한 라운드 분량의 분자는 재시드와 구별되지 않는다.**

**따라서 채널 1(라벨 피드백 → 재학습)은 강등된다.** 루프의 가치는 재학습이 아니라 **획득**에
있다. ROADMAP §4가 예측한 분기("회귀는 포화, 게이트는 미포화") 중 **게이트 쪽은 틀렸다.**

> **게이트가 포화한 것은 자기 과제이지 배포 과제가 아니다.** 학습 10 %에서 이미 ROC-AUC
> 0.993이고, 그 과제는 "JAK 활성 vs 물성 매칭된 타 타깃 활성"이다. 같은 게이트가 **실측
> 비결합자의 36 %를 통과시킨다**(§2). 0.998과 36 %는 모순이 아니다 — **다른 과제다.**
>
> **게이트의 여지는 양이 아니라 분포에 있다.** 현재 음성은 전부 "부재로 추정"이다.
> ROADMAP B(게이트 재설계)는 "음성을 더 넣기"가 아니라 **"음성의 정의를 바꾸기"**이며,
> N7은 그 항목의 근거를 옮겨 놓았지 없애지 않았다.

**§4a의 열린 질문이 닫힌다.** JAK1이 never-firing 홀드아웃에 내주는 10 %는 1,047분자 ≈
라운드 15회분 ≈ Spearman +0.0012다. **시드 노이즈(±0.0072) 아래이므로 그 손실은 공짜다.**
early_stopping 명시는 여전히 할 일이지만 **성능 사안이 아니라 일관성 사안이다.**

### 4c. 최근접이웃 베이스라인 — scaffold 분할에서 모델이 못 이긴다 (N1, 2026-08-08)

```bash
python scripts/nn_baseline_audit.py
```

베이스라인은 모델이 없다. **가장 유사한 학습 분자 하나의 실측값을 그대로 복사한다.**
지문·분할·지표는 모델과 동일하다.

| 분할 | 지표 | 모델 | 1-NN | 모델 우위 |
|---|---|---:|---:|---:|
| gap · scaffold (n=3624, 3시드) | Spearman | 0.779 ± 0.031 | **0.782 ± 0.039** | **−0.003** |
| gap · **2020 시간분할** (2534/1078) | Spearman | **0.734** | 0.572 | **+0.162** |
| potency JAK1 · scaffold | Spearman | 0.865 | 0.818 | +0.047 |

**헤드라인 Spearman 0.80이 나온 바로 그 프로토콜(scaffold 분할)에서 모델은 베이스라인을
이기지 못한다.** 차이 −0.003, 시드 산포 ±0.03~0.04.

시간 이동을 주면 둘이 갈라진다. 1-NN은 0.782 → 0.572로 무너지고 모델은 0.779 → 0.734로
버틴다. 시간분할 테스트 분자의 90 %가 신규 Murcko 스캐폴드이므로(§5) **이웃이 실제로
희박해지는 곳이 거기다.** 최근접 이웃 너머로 배운 것이 있다면 그것이 드러나는 지점이다.

> **주장의 형태가 바뀐다.** "scaffold 분할에서 Spearman 0.80"은 참이지만 조회로도 달성되므로
> **모델이 무엇을 배웠다는 증거가 아니다.** 방어 가능한 형태는 시간분할이다 —
> **"출판연도 이동 하에서 모델은 0.734를 유지하고 유사도 조회는 0.572로 떨어진다."**

**이것이 N11(평가 분할 봉인)의 선택을 정한다.** 라운드 0에서 **scaffold 분할을 봉인하면
모든 라운드가 조회와 동점인 프로토콜 위에서 채점된다.** 시간분할을 봉인해야 한다.

**한계:** 1-NN은 하나의 베이스라인이지 가장 강한 베이스라인이 아니다. 유사도 가중 k-NN은
돌리지 않았다.

### 4d. 파라로그 전이 베이스라인 (D7, 2026-08-08 측정)

```bash
python scripts/loto_audit.py     # 약 10분
```

held-out 타깃의 **같은 분자 집합**을 두 모델이 채점한다 — 자기 데이터로 학습한 것(WITHIN)과
패널의 나머지 멤버만 본 것(LOTO). LOTO는 다시 테스트 분자가 파라로그 데이터에
**존재하는지(seen)** 로 쪼갠다.

| 패널 | held out | test n | WITHIN | LOTO | LOTO seen | **LOTO unseen** | unseen n |
|---|---|---:|---:|---:|---:|---:|---:|
| jak | JAK1 | 2094 | 0.865 | 0.740 | 0.736 | **0.810** | 262 |
| jak | JAK2 | 2536 | 0.843 | 0.684 | 0.740 | **0.430** | 652 |
| jak | JAK3 | 1491 | 0.850 | 0.524 | 0.676 | **0.302** | 367 |
| pi3k | PIK3CD | 1434 | 0.816 | 0.531 | 0.609 | **0.472** | 654 |
| pi3k | PIK3CA | 1545 | 0.835 | 0.556 | 0.586 | **0.550** | 885 |
| pi3k | PIK3CB | 555 | 0.736 | 0.530 | 0.562 | **0.660** | 59 |
| pi3k | PIK3CG | 781 | 0.752 | 0.306 | 0.325 | **0.220** | 182 |

**중앙값: WITHIN 0.835 · LOTO unseen 0.472.** 새 화학에서 파라로그 전이는 자기 데이터 모델의
절반 남짓을 남긴다.

**7개 중 5개에서 unseen < seen이다.** 즉 겉보기 전이의 상당 부분이 공유 약리가 아니라
**같은 분자를 파라로그 라벨로 외운 것**이다. 예외 둘(JAK1 0.810, PIK3CB 0.660)은 unseen n이
262·59로 작아 불안정하다.

**퍼짐이 넓다 — 0.220에서 0.810.** 전이 강도는 패밀리 속성이 아니라 멤버마다 다르다.

> **PCM 연기 결정에 숫자가 붙었다(D7).** PCM의 전제는 타깃 간 공유인데, 그 공유가
> 멤버마다 0.22~0.81로 흔들린다. 타깃 7개로는 타깃 기술자 쪽을 적합할 수 없다(§9).
> **베이스라인은 낮지만 불규칙하다 — 이기기 쉬운 것이 아니라 이겼는지 판정하기 어렵다.**
>
> **G-2(chemprop 멀티태스크)도 같이 내려간다.** 멀티태스크의 전제가 태스크 간 상호 도움인데
> 새 화학에서의 태스크 간 신호가 중앙값 0.47이고, §4b는 데이터 양이 이미 포화했다고
> 측정했다. **B(게이트 음성 재정의)보다 뒤에 두어야 한다.**

**부수 관찰(가설, 측정 아님):** 전이가 가장 약한 둘은 JAK3(0.302)와 PIK3CG(0.220)다. JAK3는
§7b가 anti-target 근거가 가장 약하다고 적은 바로 그 멤버다. 두 사실이 같은 원인을 가질 수
있다 — JAK3의 SAR이 패밀리 안에서 가장 이질적이라면 "구조가 비슷하니 anti-target"이라는
추론이 그 멤버에서 가장 약해진다. **확인하려면 결합부위 수준의 근거가 필요하고 여기엔 없다.**

**한계:** 시드 1개(seed 0), 시드 산포 없음. unseen n이 작은 두 행은 특히 불안정하다.

---

## 5. 오라클 모집단 — 셌더니 대부분 비어 있었다

README가 제안했던 "스토어를 조회하는 공짜 프록시 오라클"은 **반증됐다.**

| 아이소폼 | 스토어에 pchembl 보유 | 그중 **학습셋 밖** |
|---|---:|---:|
| JAK1 | 10,463 | **11** |
| JAK2 | 12,671 | **28** |
| JAK3 | 7,452 | **16** |
| 3종 모두 측정 | 3,621 | **9** |

건초더미와 오라클이 **설계상 서로소**이기 때문이다. 인제스트를 더 해서 해결되지 않는다.

**그러나 진짜 모집단이 둘 있다:**

| | 모집단 | 답의 성격 |
|---|---|---|
| **검열 레코드** (실측 비결합자) | 아이소폼별 1,281 / 2,039 / 2,258 · **라이브러리 내 491 (드러그라이크 414)** | 상한. **전부 음성** |
| **시간 분할** (문서 연도 컷) | 2018컷 2,263 / **2020컷 1,525** | gap 실측 |

시간 분할은 사실상 스캐폴드 분할이기도 하다 — 2020컷 오라클의 Murcko 스캐폴드 587개 중
**526개(90 %)가 학습에 없던 신규 스캐폴드**다.

**저장소 전체 검열 레코드는 29,321개**이며, ChEMBL 클라이언트가 서버 사이드에서 걸러버려
그동안 손에 들어오지도 않았다.

> **두 오라클 모두 소진성 자원이다.** 라운드마다 새 답이 생기지 않는다.

---

## 6. 패널 일반화 — 명목상 되어 있고 실질은 JAK 전용

`PanelSpec` 추상화 자체는 진짜다 — 경로·캐시·모델 네임스페이스가 전부 패널에서 파생되고
`PI3K` 패널이 등록되어 있다. 문제는 그 아래다.

### 6a. 자산 격차

| 자산 | jak | pi3k |
|---|---|---|
| 아이소폼 parquet | 3 ✅ | 4 ✅ |
| `cross_measured.parquet` | ✅ (3,624행) | ✅ (1,500행) |
| `negatives` / `conformal_quantiles` / `gap_distribution` / 회귀모델 / `binder_gate` / AD 레퍼런스 | ✅ | ❌ |

### 6b. "첫 실행 시 몇 분"은 측정과 다르다

```
timeout 420 python -c "...train_and_cache(get_panel('pi3k'))"   →  Terminated (7분에 미완)
```

`binder_gate` → `build_negatives` → **10개 음성 타깃을 ChEMBL에서 라이브 fetch**(타깃당 최대
4,000 레코드). **새 패널을 세우는 데 네트워크가 필수이고 오프라인 재현 경로가 없다.**

### 6c. 라이브러리(건초더미)는 JAK 전용이다

| 패널 | 라이브러리 38,592개 중 해당 패널의 게이트 양성 |
|---|---:|
| jak | **8** (0.02 %) |
| **pi3k** | **134** (0.35 %) — 16.8배 |

라이브러리 구축 시 SMILES 배제가 **JAK 양성에 대해서만** 수행됐기 때문이다(`_gate_training_smiles`).
**§1a에서 수리한 바로 그 함수이며, 아직 패널 인자를 받지 않는다.**

### 6d. 오라클 설계가 패널 간에 이전되지 않는다

| 패널 | 라이브러리 ∩ 검열 기록만 |
|---|---:|
| jak | **491** |
| **pi3k** | **39** — JAK의 8 % |

> **오라클 계층은 "구현하면 모든 패널에서 돈다"가 아니라 패널마다 인구조사를 먼저 해야
> 하는 구조다.** PI3K에서는 검열 오라클이 사실상 성립하지 않을 가능성이 높다.

### 6e. 그 외 JAK에 묶인 지점

- `PANELS` (`src/panels.py:127`) — 하드코딩 dict 2개. 새 패밀리 = 코드 수정
- `VALIDATED_PANELS = {"jak"}` (`src/campaign.py:44`) — 손으로 유지. 의도된 설계이나 **승격 절차가 문서화되어 있지 않다**
- `NEGATIVE_TARGETS` (`src/data/negatives.py:50`) — JAK 기준 10개 바스켓. 새 패밀리에 "하드 음성"인지 검사되지 않는다
- `app.py:381,542,640,796,809` — 기본값·문구·배지가 JAK 문자열

---

## 7. 목적함수의 기록되지 않은 가정

```python
# src/selectivity.py:52 · src/funnel.py:105
S = pred[target] − max(pred[offs])
```

`JAK = PanelSpec(target="JAK1", offs=("JAK2","JAK3"))`. 이 부호가 어디서 왔는지는
**저장소 어디에도 적혀 있지 않다.** 확인된 사실: `grep -ri "disease|indication|autoimmune|
rheumatoid|myelofibrosis|unmet"` → **출력 없음. 이 저장소는 질환을 한 번도 언급하지 않는다.**

기록된 격차 셋:

| # | 격차 |
|---|---|
| **7a** | **적응증이 암묵적이다.** 목적함수의 부호는 적응증이 정한다 — 자가면역에서 JAK2는 회피 대상(EPO/TPO → 빈혈·혈소판감소)이지만, **골수증식종양에서는 JAK2가 primary 타깃**이다(JAK2 V617F). 현재 패널은 자가면역을 암묵 가정하며, 그 가정이 코드에 없다 |
| **7b** | **JAK3의 anti-target 근거가 없다.** JAK2는 라벨·기전·구조 세 근거를 갖지만 JAK3는 **구조 유사성뿐**이다. 토파시티닙이 JAK3를 치고 승인됐다. 현재 목적함수는 JAK3 결합을 근거 없이 페널티한다 |
| **7c** | **TYK2가 패널에 없다.** JAK 패밀리는 넷이다. TYK2는 IL-12/IL-23 경로를 담당하고 자가면역에서 중요하며 승인약이 있다. **선택성 계산이 패밀리의 4분의 1을 보지 않는다** |

`PI3K` 패널이 선택된 이유도 주석에 적혀 있으나 **전부 엔지니어링 근거**다(누출 규율 상속,
바스켓 비충돌). 생물학적 근거는 부연 한 절뿐이다.

---

## 8. Run History — 로그이지 상태기계가 아니다

- `registry.append_round` 호출부 **1곳**(`app.py:263`, kind=`screen`), `rounds` 읽기 **1곳**(`app.py:728`)
- `Round.kind`가 문서화한 `select` / `rescore`는 **한 번도 기록되지 않았다**
- `metrics`는 자유 dict이므로 **기계가 읽을 계약이 없다.** 라운드 N의 결과가 N+1의 선택을 바꾸지 못한다
- `append_round`는 `index = len(rounds(...))` 뒤 append하므로 **동시 실행 시 번호가 겹치고 점수 파일이 덮인다**(`src/registry.py:126`)
- **`.gitignore`에 `data/registry/`가 있다** — 주석이 "runtime state, per machine"이라고 의도를 명시한다. **케이스는 구조적으로 축적되지 않는다.** 배포 앱에서는 쓰기도 실패하며 `app.py:270`이 `OSError`를 삼킨다

---

## 9. 증거 저장소의 규모

```
assets/evidence/target.parquet  →  7 targets
```

JAK 3 + PI3K 4뿐이다. **라이브러리의 20개 타깃과 게이트 음성의 10개 타깃은 인제스트되지
않았다.** 멀티태스크·PCM 계열 작업의 전제가 여기서 걸린다.

---

## 10. 문서와 코드의 격차

계획 문서들이 이름 지은 모듈의 현재 상태:

| 계획된 파일 | 상태 |
|---|---|
| `src/oracle.py`, `src/labels.py`, `src/dmta.py`, `src/acquire.py` | **전부 없음** |
| `scripts/dmta_run.py`, `scripts/oracle_audit.py`, `scripts/funnel_falsification_audit.py` | **전부 없음** |
| `src/docking.py` | 없음 (README 모듈맵에 `future`) |

---

## 11. DMTA 한 바퀴 — 루프가 돌았다 (2026-08-09 측정)

```bash
python scripts/dmta_run.py jak jak-autoimmune 3
```

봉인 분할의 eval fold에서 교차측정 **1,078개**가 오라클 모집단이다. 모델은 train fold로
**1회 적합 후 동결**했다 — §4b가 라운드당 데이터 증가를 시드 노이즈의 1/90로 측정했으므로
재학습하는 루프는 노이즈를 학습으로 보고하게 된다. 실제로 `model_id`가 세 라운드 모두
동일하다(`CHEMBL2835@7d0c1b8807` 등). **라운드마다 바뀌는 것은 획득뿐이다.**

| 라운드 | 획득팔 적중 | 무작위팔(=base) | enrichment / 상한 | 반증률 | 중앙 오차 | 남은 모집단 |
|---:|---:|---:|---:|---:|---:|---:|
| 0 | 85.7 % | 34.3 % | 2.50× / 2.92× (**86 %**) | 7.1 % | −0.24 | 938 |
| 1 | 88.6 % | 24.3 % | 3.65× / 4.12× (**89 %**) | 1.4 % | −0.13 | 798 |
| 2 | 88.6 % | 14.3 % | 6.20× / 7.00× (**89 %**) | 4.3 % | +0.03 | 658 |

**대조팔이 무엇을 드러냈는가 — 이 표의 요점이다.**

라운드 0의 무작위팔 적중률 34.3 %는 모집단 기저율 34.0 %와 일치한다. **대조팔이 편향
없음을 스스로 증명한다.**

그리고 "advantage"가 +51.4 → +64.3 → +74.3으로 커지지만 **이것을 개선으로 읽으면 안 된다.**
획득팔은 85.7 → 88.6 → 88.6으로 사실상 평평하다. 커진 것은 **무작위팔이 34.3 → 24.3 →
14.3으로 무너졌기 때문**이고, 그 이유는 루프가 자기 손으로 선택적 분자를 먼저 꺼내가서
**남은 풀이 구조적으로 어려워졌기** 때문이다.

> **한 팔만 봤다면 "루프가 라운드마다 좋아진다"고 보고했을 것이다. 측정은 "루프가 풀을
> 소진시키고 있다"이다.** 이것이 대조팔의 값이고, Wave 1이 두 번(§2a 양성 팔, §4b 노이즈
> 바닥) 배운 것과 같은 교훈이다.

상한 대비로 정규화하면 86 % → 89 % → 89 %로 **거의 변화가 없다.** 획득의 실력은 라운드에
걸쳐 일정하다.

**획득 페널티(채널 3)의 기여는 판정 불가.** 라운드 1·2는 직전 라운드의 버킷별 반증률을
gap에서 차감했지만, 정규화 적중률이 86 → 89 → 89로 움직인 폭이 배치 70개의 표본오차
안이다. **효과가 없다는 뜻이 아니라 이 크기로는 측정되지 않는다는 뜻이다.**

**반증률 7.1 / 1.4 / 4.3 %는 추세가 없다.** §2의 93 %와 비교하면 안 된다 — 그쪽은 실측
비결합자이고 이쪽은 문헌에서 온 측정 활성이다. **모집단이 다르다.**

### 고친 계측 결함 하나

첫 실행은 `base_rate`를 두 팔을 합쳐 계산했고, 그 결과 **enrichment 2.50×가 자기 "상한"
1.67×를 넘었다.** 획득팔은 정의상 농축되어 있으므로 합치면 측정하려는 효과가 분모를
부풀린다. 기저율은 **대조팔 단독**이어야 한다. 테스트가 `enrichment <= ceiling`을 고정한다.

---

## 부록 — 재현

```bash
pip install -r requirements.txt -r requirements-dev.txt
python -m pytest -q                                    # 181 passed (2026-08-08)
# requirements-dev.txt 없이 돌리면 160 passed + 1 skipped — tests/test_db.py가
# 모듈 단위로 건너뛰어진다(duckdb는 dev 전용). 181을 보려면 dev 의존성이 필요하다.

# §1a — 수리된 생산 경로 + 재발 방지 스모크
python -m pytest tests/test_production_path.py -q
# 주의: 아래 옛 명령은 속성을 **참조만** 한다. library 쪽 파손은 함수 본문의
# import에 있었으므로 이 명령으로는 잡히지 않는다 — 호출해야 한다.
python -c "from src.data import library; library._gate_training_smiles"
python -c "import scripts.gate0_audit"

# §3 — 카이랄성 감사 (네트워크 불필요)
python scripts/chirality_audit.py

# §5, §6 — 오라클/패널 인구조사
python -m src.data.db --restore
python -c "
from src.panels import PANELS, library_molecule_overlap
for n, p in PANELS.items(): print(n, library_molecule_overlap(p))"
```

### 재현 상태 표

| 절 | 수치 | 스크립트 |
|---|---|---|
| §1a | 죽은 import | ✅ `tests/test_production_path.py` — CI 스모크 포함 (G11) |
| §2 · §2a | 93.0 % 반증, 양팔 통과율 | ✅ `scripts/funnel_falsification_audit.py` |
| §2b | 게이트 음성 A/B | ✅ `scripts/gate_ab_audit.py` |
| §3 | 카이랄성 전부 | ✅ `scripts/chirality_audit.py` |
| §4 | 하이퍼파라미터·표현 | ✅ 코드 인용 |
| §4a | early_stopping 실동작 | ✅ `scripts/early_stopping_audit.py` |
| §4b | 학습 곡선 (축별) | ✅ `scripts/learning_curve_audit.py` |
| §4c | 1-NN 베이스라인 | ✅ `scripts/nn_baseline_audit.py` |
| §4d | 파라로그 전이 (LOTO) | ✅ `scripts/loto_audit.py` |
| §0a | DMTA 준비도 | ✅ 아래 절들의 요약. 각 행에 근거 절 표기 |
| §11 | DMTA 3라운드 | ✅ `scripts/dmta_run.py` + `assets/cases/jak-autoimmune/rounds.jsonl` |
| §5 | 오라클 모집단 | ⚠️ 부분 — 질의는 재현되나 단일 스크립트 없음 |
| §6 | 패널 격차·누출 | ✅ `library_molecule_overlap` / ❌ 부트스트랩 시간 |
| §7 | 목적함수 가정 | ✅ 코드 인용 + grep |
| §8 | Run History | ✅ 코드 인용 |
| §9 | 타깃 7개 | ✅ parquet 직접 읽기 |
